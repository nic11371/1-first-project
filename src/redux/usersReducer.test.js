import usersReducer, { setUsers } from "./usersReducer";

const state = {
	users: [],
	pageSize: 15,
	totalUsersCount: 0,
	currentPage: 1,
	isFetching: false,
	followInProgress: [],
	fake: 10
};

test ('length of users should be incremented', () => {
	
})

